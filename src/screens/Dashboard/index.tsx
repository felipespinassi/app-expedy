import { View } from "native-base";
import { Text } from "react-native";

export function Dashboard() {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text>Bem vindo a Expedy, sua expedição inteligente</Text>
    </View>
  );
}
